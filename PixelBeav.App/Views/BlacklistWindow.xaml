<Window x:Class="PixelBeav.App.Views.BlacklistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PixelBeav.App.ViewModels"
        Title="Blacklist verwalten" Width="560" Height="420" WindowStartupLocation="CenterOwner">
    <Window.DataContext>
        <vm:BlacklistViewModel/>
    </Window.DataContext>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Geblacklistete Titel (erneut aufnehmen = aus Blacklist entfernen):" Margin="0,0,0,8" />

        <ListBox Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding Selected, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Aus Blacklist entfernen" IsEnabled="{Binding HasSelection}" Command="{Binding RestoreSelectedCommand}" Margin="0,0,8,0"/>
            <Button Content="Schließen" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
